<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="author" content="Michal Tichý <michal@tichy-vyvojar.cz>">
    <meta name="robots" content="noindex, nofollow">

    <title>{ifset title}{include title|stripHtml} | {/ifset}{_'app.meta.title.main'}</title>

    <link rel="stylesheet" href="{$basePath}/assets/css/bootstrap.min.css" type="text/css" media="all">
    <link rel="stylesheet" href="{$basePath}/assets/css/app.css" type="text/css" media="all">


    {block head}{/block}
</head>

<body>

<div class="container py-3">
    <header n:block="header">
    </header>

    <main>
        <div n:foreach="$flashes as $flash" n:class="'alert alert-dismissible fade show', 'alert-' . $flash->type" role="alert">
            {_$flash->message}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>

        {include content}
    </main>
</div>

<div id="overlay"></div>

{block scripts}
    <script src="{$basePath}/assets/js/bootstrap.min.js"></script>
{/block}
</body>
</html>